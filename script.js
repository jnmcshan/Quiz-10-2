var newString = document.body.querySelector(".newString");
newString.innerHTML="number"
var words = prompt("number");

var number = number(prompt("number"));

var str = "dog";

if(isNaN(number)){
  newString.innerHTML="Add number!");
  
}else{
  for(var i = 0; i < number; i++){
    str = str + words+ "dog"
  }
  newString.innerHTML=str
}